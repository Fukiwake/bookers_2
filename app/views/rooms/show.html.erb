<% @entries.each do |e| %>
  <% unless e.user.name == current_user.name %>
    <h1><%= e.user.name %> さんとのチャット</h1>
  <% end %>
<% end %>
<div id="chat_area">
  <%= render "chats/chat_area" %>
</div>
<div id="chat_form">
  <%= render "chats/chat_form" %>
</div>
